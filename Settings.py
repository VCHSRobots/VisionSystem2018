"""Settings.py manages settings.

Settings.py can retrieve the values of requested keys. It can also edit the values of keys.
Settings information is stored in VS.cfg in JSON.
"""

import json

if __name__ == "__main__":
    sys.exit("This file may not be run as a standalone.")
else:
    pass